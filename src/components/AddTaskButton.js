import React from 'react';
import './App.css';
import { Button } from 'react-bootstrap';

const AddTaskButton = () => {
  return (
    <Button>
      Ajouter une tache
    </Button>
  );
};

export default AddTaskButton;